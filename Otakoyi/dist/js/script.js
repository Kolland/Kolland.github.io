$('.slider').owlCarousel({
    loop:true,
    items: 1,
    nav:true,
    navText: [],
})

$('.events__slider').owlCarousel({
    loop:true,
    items: 2,
    dots:true,
    margin: 60,
    // navText: [],
    autoWidth   : true,
})

$('.reviews__slider-box').owlCarousel({
    // loop:true,
    items: 1,
    dots:true,
    margin: 10,
    // autoWidth   : true,
    // navText: [],
})